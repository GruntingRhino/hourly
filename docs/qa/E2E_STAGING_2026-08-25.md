# GoodHours Staging E2E Functional Pass — 2026-08-25

**Target:** https://hourly-dev.vercel.app (staging)
**Method:** curl against `/api/*` with synthetic identities (`example.invalid`), seeded staging accounts, negative-path probes. Continuation of delegated run `deleg_9738f9a9` (which found B1/B2 below).
**Overall verdict:** ❌ NOT LAUNCH-READY — staging is blocked by stale deployment + widespread 500s on core discovery/signup surfaces.

## Environment findings

| # | Finding | Evidence |
|---|---------|----------|
| E1 | **Deployed API is an older build than `launch-prep` HEAD.** QR attendance (`POST /api/sessions/opportunities/:id/qr-token`, `POST /api/sessions/qr-checkin`), supervisor verification consume (`POST /api/beneficiaries/supervisor-verification/:token/consume`), `GET /api/verification/pending`, and `GET /api/cohorts/export` all return the app-level fallback `404 {"error":"Not found"}` despite existing in server source. | curl probes 2026-08-25 |
| E2 | **Widespread 500 on every endpoint querying Opportunity / Organization / Signup / ServiceSession tables** (public opportunities list, opportunity by id, signups/my, sessions/my GET, organizations list). Endpoints touching only User/School/Beneficiary work (auth/me 200, beneficiaries list 200, tier 200). Suspected unapplied DB migrations / schema drift on staging Neon DB. | curl probes |

## Flow matrix

| Flow | Endpoint(s) | Result | Status |
|------|-------------|--------|--------|
| Health check | `GET /` | 200 `{"status":"ok","db":"ok"}` | ✅ PASS |
| Register org admin | `POST /api/auth/signup` role ORG_ADMIN | **Impossible by design**: `VALID_ROLES = ["SCHOOL_ADMIN"]` — no public path creates ORG_ADMIN | ⛔ UNTESTABLE (gap) |
| Create organization | no public POST /api/organizations create route | ⛔ UNTESTABLE (gap) |
| Create opportunity | `POST /api/opportunities` (ORG_ADMIN) | blocked: no ORG_ADMIN account exists on staging | ⛔ UNTESTABLE |
| Student signup for opportunity | `POST /api/signups` | validation layer works (400 without opportunityId) but cannot proceed: opportunities list/detail 500 (E2); seeded student got `{"error":"Student school not found"}` from invitation path | ❌ FAIL (blocked by E2) |
| Login (seeded STUDENT / BENEFICIARY_ADMIN) | `POST /api/auth/login` | 200 + JWT both roles | ✅ PASS |
| Login (seeded SCHOOL_ADMIN admin@lincoln.edu) | `POST /api/auth/login` | **500 Internal server error** (reproduced twice, two runs) | ❌ FAIL (BUG-0825-3) |
| New user registration (SCHOOL_ADMIN) | `POST /api/auth/signup` | 400 weak-password validation OK; valid payload → **500 Internal server error** across 3 fresh emails; retry after cooldown → 500 not 409 (no row persisted) | ❌ FAIL (BUG-0825-2) |
| Signup abuse controls | signup rate limit | 429 RATE_LIMITED fires correctly; fixed-window cleared on schedule (~60 min, verified live); X-Forwarded-For spoofing correctly ignored | ✅ PASS |
| Forgot password anti-enumeration | `POST /api/auth/forgot-password` | 200 constant response | ✅ PASS |
| Verify-email bad token | `GET /api/auth/verify-email` | 400 zod validation | ✅ PASS |
| Unauthenticated access control | `/api/auth/me`, import-csv, schools directory | 401 consistently | ✅ PASS |
| RBAC role gates | student→ORG_ADMIN route, student→verification/pending | 403/404 correctly denied | ✅ PASS |
| Beneficiary directory (BENEFICIARY_ADMIN) | list/tier | 200 correct payloads | ✅ PASS |
| QR attendance generate/check-in incl. expiry + replay | qr-token / qr-checkin | **404 — routes not in deployed build** (E1). Expiry & replay rejection untestable | ⛔ UNTESTABLE (stale deploy) |
| Supervisor verification link flow | supervisor-verification endpoints | generate requires linked beneficiary (BA token → 404 "Beneficiary not found"); consume endpoint 404 not deployed (E1) | ⛔ UNTESTABLE |
| CSV export | `GET /api/reports/export/csv` (student) → 503 service-unavailable error message; `GET /api/cohorts/export` → 404 not deployed | ❌ FAIL / partial |
| CSV import roundtrip + rollback | `POST /api/beneficiaries/import-csv`, `POST /api/cohorts/:id/import` | import-csv route present (401/403 auth gates pass) but roundtrip blocked: no usable SCHOOL_ADMIN session (login 500) | ⛔ UNTESTABLE (blocked by BUG-0825-3) |
| Invitation accept (student) | invalid token | 404 Invalid invitation token | ✅ PASS (negative) |

## Bugs filed this run

See `BUGS.md` entries BUG-0825-1 … BUG-0825-4 (2026-08-25 section).

## Untestable summary & what unblocks it

1. Deploy current `launch-prep` server build to staging (fixes E1 → enables QR attendance expiry/replay tests, supervisor verification, cohort CSV export).
2. Run pending Prisma migrations on staging DB (fixes E2 → enables opportunity browse/signup/session flows).
3. Provide a seeded ORG_ADMIN account (or restore ORG_ADMIN signup path) to test org-admin → opportunity → QR chain end-to-end.
4. Fix SCHOOL_ADMIN login 500 (BUG-0825-3) to enable CSV import/export roundtrip tests.
